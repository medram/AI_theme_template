const Configs = {
    particles: false,               // enable/disable jumbotron particles (in home page)
    page_header_particles: true     // enable/disable page headers particles
};

export default Configs;
